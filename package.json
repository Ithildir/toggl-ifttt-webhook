{
  "name": "ifttt-webhook",
  "version": "1.0.0",
  "private": true,
  "description": "Simple IFTTT webhook with some useful features",
  "keywords": [
    "ifttt",
    "toggl",
    "webhook"
  ],
  "homepage": "https://github.com/Ithildir/ifttt-webhook#readme",
  "bugs": {
    "url": "https://github.com/Ithildir/ifttt-webhook/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Ithildir/ifttt-webhook.git"
  },
  "license": "MIT",
  "author": "Andrea Di Giorgi",
  "scripts": {
    "lint": "prettier --check . && sort-package-json --check",
    "lint-fix": "prettier --write . && sort-package-json",
    "test": "ava",
    "test-coverage": "nyc --reporter=lcov --reporter=text npm test"
  },
  "prettier": {
    "singleQuote": true
  },
  "ava": {
    "require": [
      "dotenv/config"
    ]
  },
  "dependencies": {
    "toggl-api": "^1.0.1"
  },
  "devDependencies": {
    "ava": "^3.5.2",
    "dotenv": "^8.2.0",
    "eslint": "^6.8.0",
    "nock": "^12.0.3",
    "nyc": "^15.0.1",
    "prettier": "^2.0.2",
    "sinon": "^9.0.2",
    "sort-package-json": "^1.40.0"
  }
}
